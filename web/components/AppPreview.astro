---
/** Animated CSS mockup of the gpui-starter desktop app. Cycles through 5 frames. */
---

<div class="desktop-mockup mx-auto max-w-2xl">
  <!-- Title bar -->
  <div class="mock-titlebar">
    <div class="flex items-center gap-1.5">
      <span class="mock-dot" style="background:#ff5f57"></span>
      <span class="mock-dot" style="background:#febc2e"></span>
      <span class="mock-dot" style="background:#28c840"></span>
    </div>
    <span class="mock-title">gpui-starter</span>
    <div class="w-12"></div>
  </div>

  <!-- Window body -->
  <div class="mock-body">
    <!-- Sidebar -->
    <div class="mock-sidebar">
      <div class="sidebar-item active" data-frame-active="1">
        <svg width="12" height="12" viewBox="0 0 16 16" fill="currentColor"><path d="M8 1l7 6v8H1V7z"/></svg>
        <span data-label="home">Home</span>
      </div>
      <div class="sidebar-item" data-frame-active="2,3,4,5">
        <svg width="12" height="12" viewBox="0 0 16 16" fill="currentColor"><rect x="2" y="2" width="12" height="12" rx="2"/></svg>
        <span data-label="form">Form</span>
      </div>
      <div class="sidebar-item" data-frame-active="2,3,4,5">
        <svg width="12" height="12" viewBox="0 0 16 16" fill="currentColor"><circle cx="8" cy="8" r="6"/></svg>
        <span data-label="settings">Settings</span>
      </div>
      <div class="sidebar-item">
        <svg width="12" height="12" viewBox="0 0 16 16" fill="currentColor"><path d="M8 1a7 7 0 100 14A7 7 0 008 1zm0 2a5 5 0 110 10A5 5 0 018 3z"/></svg>
        <span data-label="about">About</span>
      </div>
    </div>

    <!-- Content area -->
    <div class="mock-content">
      <!-- Frame 1: Home -->
      <div class="app-frame">
        <div class="frame-header">Welcome to GPUI Starter</div>
        <div class="frame-sub">A boilerplate for building desktop apps with GPUI</div>
        <div class="frame-stats">
          <div class="stat-pill"><span class="stat-val">24</span> themes</div>
          <div class="stat-pill"><span class="stat-val">2</span> languages</div>
          <div class="stat-pill"><span class="stat-val">100%</span> Rust</div>
        </div>
      </div>

      <!-- Frame 2: Theme picker -->
      <div class="app-frame">
        <div class="frame-header">Settings</div>
        <div class="flex items-center justify-between mb-3">
          <span class="text-[10px] text-fg-muted">Dark Mode</span>
          <div class="toggle-on"><div class="toggle-knob"></div></div>
        </div>
        <div class="text-[10px] text-fg-muted mb-2">Theme</div>
        <div class="flex flex-wrap gap-1.5">
          <span class="theme-tag active-tag">Catppuccin</span>
          <span class="theme-tag">Tokyo Night</span>
          <span class="theme-tag">Dracula</span>
          <span class="theme-tag">One Dark</span>
        </div>
      </div>

      <!-- Frame 3: Command Launcher -->
      <div class="app-frame">
        <div class="cmd-bar">
          <svg width="10" height="10" viewBox="0 0 16 16" fill="currentColor" class="text-accent"><circle cx="7" cy="7" r="5.5" fill="none" stroke="currentColor" stroke-width="2"/><path d="M11 11l3.5 3.5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
          <span class="text-fg-muted text-[10px]">Search pages and commands...</span>
          <span class="cursor-blink"></span>
        </div>
        <div class="cmd-results">
          <div class="cmd-row active">
            <div class="cmd-icon">⌂</div>
            <div><div class="text-[10px] font-medium text-fg">Home</div><div class="text-[8px] text-fg-subtle">Open the Home page</div></div>
          </div>
          <div class="cmd-row">
            <div class="cmd-icon dim">☆</div>
            <div><div class="text-[10px] text-fg-muted">Form</div><div class="text-[8px] text-fg-subtle">Open the Form page</div></div>
          </div>
          <div class="cmd-row">
            <div class="cmd-icon dim">⚙</div>
            <div><div class="text-[10px] text-fg-muted">Settings</div><div class="text-[8px] text-fg-subtle">Open settings</div></div>
          </div>
        </div>
        <div class="cmd-footer">
          <span>↑↓ navigate</span><span>↵ open</span><span>esc close</span>
        </div>
      </div>

      <!-- Frame 4: i18n -->
      <div class="app-frame">
        <div class="frame-header">设置</div>
        <div class="flex items-center justify-between mb-3">
          <span class="text-[10px] text-fg-muted">深色模式</span>
          <div class="toggle-on"><div class="toggle-knob"></div></div>
        </div>
        <div class="text-[10px] text-fg-muted mb-2">语言</div>
        <div class="flex flex-wrap gap-1.5">
          <span class="theme-tag">English</span>
          <span class="theme-tag active-tag">简体中文</span>
        </div>
      </div>

      <!-- Frame 5: Form validation -->
      <div class="app-frame">
        <div class="frame-header">Create Account</div>
        <div class="space-y-2.5">
          <div>
            <div class="field-label">Full Name</div>
            <div class="field-box">John Doe</div>
          </div>
          <div>
            <div class="field-label">Email</div>
            <div class="field-box error">invalid</div>
            <div class="field-error">Enter a valid email address</div>
          </div>
          <div>
            <div class="field-label">Password</div>
            <div class="field-box">••••</div>
          </div>
          <div class="flex gap-2 pt-1">
            <span class="btn-sm-primary">Create Account</span>
            <span class="btn-sm-ghost">Reset</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .desktop-mockup {
    border: 1px solid var(--color-rule);
    background: var(--color-code);
    overflow: hidden;
  }

  .mock-titlebar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 14px;
    background: var(--color-panel);
    border-bottom: 1px solid var(--color-line);
  }

  .mock-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    display: inline-block;
  }

  .mock-title {
    font-size: 11px;
    font-weight: 600;
    color: var(--color-fg-subtle);
    letter-spacing: 0.02em;
  }

  .mock-body {
    display: flex;
    min-height: 260px;
  }

  .mock-sidebar {
    width: 72px;
    padding: 12px 8px;
    background: var(--color-panel);
    border-right: 1px solid var(--color-line);
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .sidebar-item {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 6px 8px;
    font-size: 9px;
    color: var(--color-fg-subtle);
    transition: all 0.2s;
  }

  .sidebar-item.active {
    color: var(--color-accent);
    background: color-mix(in srgb, var(--color-accent) 12%, transparent);
  }

  .mock-content {
    flex: 1;
    padding: 16px;
    position: relative;
    overflow: hidden;
  }

  .app-frame {
    position: absolute;
    inset: 0;
    padding: 16px;
    opacity: 0;
    transform: translateY(6px);
    animation: app-frame-cycle 25s ease-in-out infinite;
  }

  .app-frame:nth-child(1) { animation-delay: 0s; }
  .app-frame:nth-child(2) { animation-delay: 5s; }
  .app-frame:nth-child(3) { animation-delay: 10s; }
  .app-frame:nth-child(4) { animation-delay: 15s; }
  .app-frame:nth-child(5) { animation-delay: 20s; }

  @keyframes app-frame-cycle {
    0%, 16%   { opacity: 0; transform: translateY(6px); }
    18%, 34%  { opacity: 1; transform: translateY(0); }
    36%, 100% { opacity: 0; transform: translateY(-6px); }
  }

  .frame-header {
    font-size: 13px;
    font-weight: 700;
    color: var(--color-fg);
    margin-bottom: 10px;
  }

  .frame-sub {
    font-size: 10px;
    color: var(--color-fg-muted);
    margin-bottom: 14px;
  }

  .frame-stats {
    display: flex;
    gap: 8px;
  }

  .stat-pill {
    font-size: 9px;
    color: var(--color-fg-muted);
    padding: 4px 10px;
    background: var(--color-panel);
    border: 1px solid var(--color-line);
  }

  .stat-val {
    color: var(--color-accent);
    font-weight: 700;
  }

  .toggle-on {
    width: 28px;
    height: 14px;
    border-radius: 999px;
    background: var(--color-accent);
    position: relative;
  }

  .toggle-knob {
    position: absolute;
    right: 2px;
    top: 2px;
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--color-canvas);
  }

  .theme-tag {
    font-size: 9px;
    padding: 3px 8px;
    border: 1px solid var(--color-line);
    color: var(--color-fg-muted);
  }

  .active-tag {
    border-color: var(--color-accent);
    color: var(--color-accent);
    background: color-mix(in srgb, var(--color-accent) 12%, transparent);
  }

  .cmd-bar {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    border: 1px solid var(--color-line);
    background: var(--color-canvas);
    margin-bottom: 6px;
  }

  .cmd-results {
    padding: 4px;
    margin-bottom: 6px;
  }

  .cmd-row {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 6px 8px;
  }

  .cmd-row.active {
    background: color-mix(in srgb, var(--color-accent) 12%, transparent);
  }

  .cmd-icon {
    width: 18px;
    height: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: color-mix(in srgb, var(--color-accent) 12%, transparent);
    font-size: 10px;
    color: var(--color-accent);
  }

  .cmd-icon.dim {
    background: var(--color-panel);
    color: var(--color-fg-subtle);
  }

  .cmd-footer {
    display: flex;
    gap: 14px;
    padding: 6px 12px;
    border-top: 1px solid var(--color-line);
    font-size: 8px;
    color: var(--color-fg-subtle);
  }

  .field-label {
    font-size: 9px;
    color: var(--color-fg-muted);
    margin-bottom: 3px;
  }

  .field-box {
    height: 22px;
    border: 1px solid var(--color-line);
    background: var(--color-canvas);
    padding: 0 8px;
    display: flex;
    align-items: center;
    font-size: 10px;
    color: var(--color-fg);
  }

  .field-box.error {
    border-color: #f87171;
  }

  .field-error {
    font-size: 8px;
    color: #f87171;
    margin-top: 2px;
  }

  .btn-sm-primary {
    font-size: 9px;
    padding: 4px 10px;
    background: var(--color-accent);
    color: var(--color-canvas);
    font-weight: 700;
  }

  .btn-sm-ghost {
    font-size: 9px;
    padding: 4px 10px;
    border: 1px solid var(--color-line);
    color: var(--color-fg-muted);
  }

  .cursor-blink {
    display: inline-block;
    width: 1px;
    height: 12px;
    background: var(--color-accent);
    margin-left: auto;
    animation: blink 1s steps(1) infinite;
  }

  @keyframes blink {
    50% { opacity: 0; }
  }

  @media (prefers-reduced-motion: reduce) {
    .app-frame {
      animation: none;
      position: relative;
      opacity: 1;
      transform: none;
    }
    .app-frame:nth-child(n+2) { display: none; }
  }
</style>
